# HRRR Cases

This directory will host small target-area case definitions driven by the
canonical ingest pipeline. The first real-data cold-start workflow is deferred
until the ingest IR and dry/moist runtime contracts are stable.
